server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: https://github.com/D4ig0/Tingeso-Pep-3
          search-paths: config-data
  application:
    name: config-service